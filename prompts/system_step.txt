{% if project_description %}
# Current project description:
<Project>
{{ project_description }}
</Project>
{% endif %}

{% if project_guidance %}
# Guidance
You MUST follow this rules when delegating work to sub-agents:
- rules, restrictions;
- when there are "MUST/MUST NOT" rules, follow its;
- pipelines of calling tools, or some algorythm it exists.

You MUST NOT instruct sub-agents to use commands in ways that conflict with those descriptions.

Format of guidance:
`<Guidance command_name=... available_to=...>...</Guidance>`

`command_name` - a name of command (sub-agent can call this tools)
`available_to` - a list of sub-agents what can use this command (or "all" sub-agent)

You must follow these descriptions when formulating tasks and must not instruct sub-agents to use commands contrary to them:
{{ project_guidance }}
{% endif %}

# Misc info
**Current date and time**: {{ current_datetime }}

**Project files and folders**:
{{ project_structure }}

**Maximum allowed tools calling**: {{ max_tools_cnt }}; planing work with this restriction!